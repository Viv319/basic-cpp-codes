#include<bits/stdc++.h>
using namespace std;

int main()
{
	int i;
	string s="my new string";
	int a =s.length();
	cout<<" length of string is "<<a<<"\n";
	
	string s1="MY NEW STRING";
	
	transform(s.begin(),s.end(),s.begin(),::tolower);
  //used to convert capital letters to small
  
	cout<< "first string \n "<<s<<"\n second string \n"<<s1;
	int b=s1.length();
	
  transform(s1.begin(),s1.end(),s1.begin(),::tolower);
	if(s.compare(s1)==0)
  cout<< "first string is equal to second \n";
	//prebuid compare function to compare two different strings if result is 0 then it's match else it is not a match
  else
	cout<< "first string is not equal to second \n";
		
}
